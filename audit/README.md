# 审查与修复材料（audit/）

stock-trends 缺陷审查与修复的完整材料。审查日期 2026-09-18，基线 commit `6a4c796`（main）。
本目录随 PR `fix/audit-defects` 一并提交，使审阅**自包含**——无需外部文件即可复核。

## 建议阅读顺序

1. **`审查报告.md`** — 缺陷清单与测试结论（致命 4 / 严重 5 / 中 4 / 重构 9）。
2. **`改动位置标注.md`** — 变更定位：文件清单、按缺陷分组的行号定位表、关键改动前后对照、3 处需重点确认的设计决定（**主审阅材料**）。
3. **`验收对照.md`** — 修复项 ↔ 验收结论，逐条映射到测试用例。
4. **`diffs/`** — 标准 unified diff，逐行差异，可直接 `git apply`。
5. 完整源码即**仓库根目录**的 `data.js` / `script.js` / `.btest.cjs` / `.itest.cjs`（本目录不重复放整份源码）。

## 文件清单

| 文件 | 说明 |
|---|---|
| `审查报告.md` | 代码审查与缺陷测试报告 |
| `改动位置标注.md` | 改动定位与前后对照（**主要审阅材料**） |
| `验收对照.md` | 修复项 ↔ 验收结论对照表 |
| `diffs/data.js.diff` | 620 行补丁 |
| `diffs/script.js.diff` | 174 行补丁 |
| `diffs/btest.cjs.diff` | 439 行补丁（新增文件，等价于完整内容） |
| `diffs/itest.cjs.diff` | 78 行补丁（修复测试进程不退出） |

## 改动规模（基线 `6a4c796` → PR `fix/audit-defects`）

| 文件 | 基线 | 改动后 |
|---|---|---|
| `data.js` | 2337 行 | 2676 行 |
| `script.js` | 1544 行 | 1599 行 |
| `.itest.cjs` | 169 行 | 213 行 |
| `.btest.cjs` | —（新增） | 433 行 |

> `dist/` 是源文件的**纯拷贝**（该项目 `package.json` 无 scripts、无构建步骤）。改源码必须同步 `dist/`，否则线上仍是旧代码——本 PR 已同步。

## 验证命令

```bash
node .btest.cjs     # 期望：通过 21 项，失败 0 项，退出码 0
node .itest.cjs     # 期望：全部通过，退出码 0（已修复"跑完不退出"，现能主动退出）
```

## 修复范围

- **4 个致命缺陷**：① 抓取失败清空热度基线 ② 哈希伪造涨跌幅 ③ 自动画像风控空转 + 止损位不可执行 ④ 连续搜索响应竞态
- **3 项严重缺陷**：RSI 45–55 判定死区、`target2` 与 `target1` 几乎重合、模糊匹配越界

其中 ①→`HotStore` 模块、②→变化率策略表 `DELTA_STRATEGY`、③→价位策略 `LEVELS_STRATEGY` + 风控规则引擎 `RISK_RULES`/`SCORE_RULES`、④→请求身份闸门 `RequestGate`。
