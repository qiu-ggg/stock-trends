# 验收对照（修复项 ↔ 验收结论）

> 验收以 **`node .btest.cjs`**（缺陷定向测试）为准；集成层以 `.itest.cjs` 为准。
> 基线 commit `6a4c796`（main）→ PR `fix/audit-defects`。

| # | 缺陷 | 严重度 | 修复要点 | 代码位置 | 对应验收用例（`.btest.cjs`） | 结论 |
|---|---|---|---|---|---|---|
| 1 | 抓取失败清空热度基线 | 致命 | `HotStore` + 写入门禁 | `data.js` | T2 ·「抓取失败（空池）时不应覆盖已有热度基线」/「空池后基线仍在，恢复抓取时按真实基线计算变化率」 | ✅ 通过 |
| 2 | 无基线伪造涨跌幅 | 致命 | `DELTA_STRATEGY`；无基线返回 `null` | `data.js` | T2 ·「无历史基线时 delta 为 null 并标注来源（不再用哈希伪造数值）」 | ✅ 通过 |
| 3 | 自动画像风控空转 + 止损不可执行 | 致命 | `LEVELS_STRATEGY` + `RISK_RULES`/`SCORE_RULES`；止损锚定绝对支撑 | `data.js` | T1 ·「计划输出携带价位来源与静态性（判定与展示同源）」/「随动推导的止损位必须附带披露文案」/「存在可锚定绝对支撑的样本」/「现价已明显离开计划入场区间时，盈亏比须标注不可执行」 | ✅ 通过 |
| 4 | 搜索响应竞态 | 致命 | `RequestGate`（四通道） | `script.js` | T7 ·「后发请求（五粮液）胜出，先发的慢响应不应覆盖后发结果」 | ✅ 通过 |
| 5 | RSI `(45, 55)` 死区 | 严重 | 档位表 + 末档无条件兜底 | `data.js`、`script.js` | T3 ·「RSI 落在 45–55（非超买非超卖区）时应输出一条中性信号」 | ✅ 通过 |
| 6 | `target2` 与 `target1` 几乎重合 | 严重 | `minTgtGap` 最小间距 | `data.js` | T6 ·「两档目标位应有可操作间距（不应几乎重合）」（阈值 2%，修复前实测 0.05%） | ✅ 通过 |
| 7 | 模糊匹配越界 | 严重 | 长度下限 2 + 多命中返 `ambiguous` + 确定性排序 | `data.js`、`script.js` | T4 ·「多义关键词应返回歧义候选清单」/「单字符/单字母关键词不再命中任意股票」/「唯一模糊命中仍可正常解析」/「精确匹配能力保持不变」/「同一输入重复调用结果一致」 | ✅ 通过 |
| 附 | K 线样本不足 / 12–40 根区间 | 中 / 严重 | 降级路径与字段兜底 | `data.js`、`script.js` | T5 ·「样本不足 12 根时返回 error 对象而非抛异常」/「空 K 线不崩溃」；T8 ·「12–40 根样本应稳定输出」 | ✅ 通过 |
| 附 | `.itest.cjs` 进程不退出 | — | 登记并清理遗留定时器 + 显式 `process.exit` | `.itest.cjs` | 运行 `node .itest.cjs` 能主动退出（退出码 0） | ✅ 通过 |

## 总体验收结论

| 维度 | 结果 |
|---|---|
| `.btest.cjs` 缺陷定向测试 | **21 / 21 通过**（修复前失败 9 项） |
| `.itest.cjs` 集成测试 | **全部通过**（且本次修复了其「跑完不退出」问题） |
| 反向验证 | 修复前运行同一缺陷测试失败 9 项，与缺陷清单逐条吻合 → 断言有真实鉴别力 |
